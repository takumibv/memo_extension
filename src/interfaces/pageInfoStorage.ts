import { PageInfo } from "../types/PageInfo";

export const createPageInfo = async (PageInfo: PageInfo) => {};

export const updatePageInfo = async (PageInfo: PageInfo) => {};

export const getAllPageInfos = async () => {};

export const getPageInfosById = async (pageId: number) => {};

export const deletePageInfo = async (PageInfoId: number) => {};
